import { Container } from '@mantine/core';

import { Hero } from './ui';

export const IndexPage = () => {
    return (
        <section>
            <Container>
                <Hero />
            </Container>
        </section>
    );
};
