import { Button } from '@mantine/core';

export const TakeTestAgain = () => {
    return (
        <Button variant='default' bg='transparent' size='md' c='dark.9' color='dark.9'>
            Пройти тест ещё раз
        </Button>
    );
};
